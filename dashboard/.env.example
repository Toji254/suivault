# SuiVault Agent Environment Variables

# The private key of the agent (Bech32 encoded, starts with suiprivkey)
# If left empty, the active-agent script will generate a temporary one for you.
AGENT_SECRET_KEY=

# Optional: Override the network if needed (testnet is default)
# SUI_NETWORK=testnet
